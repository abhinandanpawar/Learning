package com.tenx.ds;

import java.util.Arrays;
import java.util.List;
import java.util.Map;
import java.util.HashMap;

public class WordFrequency {
    public static void main(String[] args) {
        String sentence = "The quick brown fox jumps over the lazy dog";

        // Your Mission:
        // 1. Split the sentence into a list of words.
        //    Hint: Use sentence.toLowerCase().split(" ");
        // 2. Create a HashMap to store word frequencies.
        //    The key should be the word (String), and the value should be the count (Integer).
        // 3. Loop through the list of words. For each word, update its count in the HashMap.
        //    Hint: Use map.getOrDefault(word, 0) + 1 to handle words you haven't seen before.
        // 4. Print out the final frequency map.

        // --- Your code goes here ---


        // --- End of your code ---
    }
}
